---------------------!HOW TO RUN THE PROGRAM!---------------------

->	In command promt or VS Code change the directory to this project directory.
->	Then type 'python app.py' in the terminal to run the app.
->	After starting the program the URL for the local server will be given at the bottom of
	the terminal.
->	Then type the URL in the browser. 
